= M*nads

Control monad voor error handling bij zoveel GameRunner als GameParser

State monad voor toevoegen van connecties

State monad voor filling van interface, return resulting interface in case it is needed for future alignment

Map idea : spider web
